# Architecture of the project
## 1 ) Project folder tree:
    Any files and folders of the project should be CapitalCase
    Components are in src/Components
    any components should be a file and/or stored in a folder with the name of the component, if there are components used only for one components they should be in the same folder